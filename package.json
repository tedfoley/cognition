{
  "name": "codeql-remediation-orchestrator",
  "version": "1.0.0",
  "description": "GitHub Action that identifies and fixes CodeQL security issues using Devin API with intelligent batching and real-time dashboard",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && ncc build dist/index.js -o dist-action",
    "test": "jest",
    "lint": "eslint src/**/*.ts",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "github-action",
    "codeql",
    "security",
    "devin",
    "automation"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@octokit/rest": "^20.0.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.2"
  }
}
